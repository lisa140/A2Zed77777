// Payment callback page — no longer used in the inline modal flow.
// Flutterwave's useFlutterwave hook handles the full payment cycle inline.
export default function PaymentCallbackPage() {
  return null;
}
